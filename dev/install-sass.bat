:: Change current working directory to parent directory (project main directory)
cd ..

:: Install sass as a global (not package specific) npm dependency so "monitor-sass.bat" works
npm install -g sass